package com.example.myapplication;

import android.Manifest;
import android.content.Context;
import android.content.Intent;
import android.content.pm.PackageManager;
import android.net.wifi.WifiManager;
import android.support.v4.app.ActivityCompat;
import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.widget.Toast;

public class signal_power extends AppCompatActivity {

    /*

    public Button history_3;
    public Button wifi_list_3;
    public Button signal_power_3;

    public void init()
    {
        history_3 = (Button) findViewById(R.id.history_3);
        history_3.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View view) {
                Intent toy = new Intent(signal_power.this, history.class);
                startActivity(toy);
            }
        });

        wifi_list_3 = (Button) findViewById(R.id.network_3);
        wifi_list_3.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View view) {
                Intent main = new Intent(signal_power.this, MainActivity.class);
                startActivity(main);
            }
        });
    }

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);

        history_3 = (Button) findViewById(R.id.history_3);
        history_3.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View view) {
                Intent toy = new Intent(signal_power.this, history.class);
                startActivity(toy);
            }
        });

        wifi_list_3 = (Button) findViewById(R.id.network_3);
        wifi_list_3.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View view) {
                Intent main = new Intent(signal_power.this, MainActivity.class);
                startActivity(main);
            }
        });
    }

     */
}